# Dockerfile

> Bu dosya henüz doldurulmadı — Dockerfile'ın kendisini (nasıl yazılır, hangi komutlar var) öğrendiğimde buraya işleyeceğim.

## Öğrenmem gerekenler
- [ ] Dockerfile nedir, ne işe yarar
- [ ] Temel komutlar: `FROM`, `RUN`, `COPY`, `ADD`, `CMD`, `ENTRYPOINT`, `WORKDIR`, `EXPOSE`, `ENV`
- [ ] `CMD` ile `ENTRYPOINT` arasındaki fark
- [ ] `.dockerignore` ne işe yarar
- [ ] Multi-stage build nedir, neden kullanılır
- [ ] `docker build` komutunun detaylı kullanımı (`-t`, `-f`, build context kavramı)

## Notlarım
> (Buraya ekleyeceğim)
